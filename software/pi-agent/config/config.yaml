app:
  name: "FilamentTracker Live Spool"
  version: "0.1.0"
  environment: "development"

server:
  host: "127.0.0.1"
  port: 8001

device:
  name: "Live Spool"
  location: "Workshop"

hardware:
  device_mode: "mock"

scale:
  enabled: true
  mock: true
  unit: "grams"

nfc:
  enabled: true
  mock: true
