app:
  name: "FilamentTracker Live Spool"
  version: "0.1.0"
  environment: "development"

server:
  host: "127.0.0.1"
  port: 8001

device:
  name: "Live Spool"
  location: "Workshop"

hardware:
  device_mode: "mock"

scale:
  enabled: true
  backend: "mock"
  mock: true
  unit: "grams"
  hx711:
    data_pin: 5
    clock_pin: 6
    gain: 128
    samples: 15

nfc:
  enabled: true
  mock: true
